import { metadataForPath } from "@/lib/route-metadata";

export const metadata = metadataForPath("/capabilities/ai-voice-chat-experiences");

export default function AIVoiceChatExperiencesLayout({ children }: { children: React.ReactNode }) {
  return children;
}

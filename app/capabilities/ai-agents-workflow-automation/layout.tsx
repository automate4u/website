import { metadataForPath } from "@/lib/route-metadata";

export const metadata = metadataForPath("/capabilities/ai-agents-workflow-automation");

export default function AIAgentsWorkflowAutomationLayout({ children }: { children: React.ReactNode }) {
  return children;
}

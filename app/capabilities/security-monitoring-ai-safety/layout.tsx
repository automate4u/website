import { metadataForPath } from "@/lib/route-metadata";

export const metadata = metadataForPath("/capabilities/security-monitoring-ai-safety");

export default function SecurityMonitoringAISafetyLayout({ children }: { children: React.ReactNode }) {
  return children;
}

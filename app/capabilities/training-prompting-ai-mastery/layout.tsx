import { metadataForPath } from "@/lib/route-metadata";

export const metadata = metadataForPath("/capabilities/training-prompting-ai-mastery");

export default function TrainingPromptingAIMasteryLayout({ children }: { children: React.ReactNode }) {
  return children;
}

import { metadataForPath } from "@/lib/route-metadata";

export const metadata = metadataForPath("/capabilities/custom-software-ai-solutions");

export default function CustomSoftwareAISolutionsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
